import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PdfGenerator = () => {
  return (
    <View>
      <Text>PdfGenerator</Text>
    </View>
  );
};

export default PdfGenerator;

const styles = StyleSheet.create({});
